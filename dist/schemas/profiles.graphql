extend type Query {
  profiles: [Profile]
  profile(id: String!): Profile
}

type Profile {
  id: ID!
  name: String!
  about: JSON!
  draft: JSON!
  users: [User]
  posts: [Post]
  memberships: [Community]
  createdAt: DateTime!
  updatedAt: DateTime!
}
